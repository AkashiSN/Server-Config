---
# file: roles/delete/tasks/minecraft.yml
- name: Delete the minecraft/minecraft manifest
  kubernetes.core.k8s:
    state: absent
    template: manifests/minecraft/minecraft.yml
    wait: true
  become: false

- name: Delete the minecraft/persistent-volumes manifest
  kubernetes.core.k8s:
    state: absent
    template: manifests/minecraft/persistent-volume.yml
  become: false
