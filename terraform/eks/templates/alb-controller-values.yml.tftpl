clusterName: ${cluster_name}
region: ${aws_region}
vpcId: ${vpc_id}
serviceAccount:
  create: true
  name: aws-load-balancer-controller
  annotations:
    eks.amazonaws.com/role-arn: ${alb_controller_sa_role_arn}
